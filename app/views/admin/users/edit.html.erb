<h1>Editing User</h1>

<%= error_messages_for :user %>

<% form_for(:user, :url => object_url, :html => { :method => :put, :class => "validate" }) do |f| %>
  <%= render :partial => "form", :locals => { :f => f } %>
  <p class="submit"><%=submit_tag "Update"%></p>
<% end %>

<br/>

<%= link_to 'Back', collection_url %>